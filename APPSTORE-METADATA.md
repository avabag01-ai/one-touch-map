# 원터치맵 — App Store 등록 메타데이터 (복붙용)

App Store Connect에서 앱 만들 때 아래 값들을 그대로 붙여넣으면 됨.
언어: 기본 **한국어(대한민국)**.

---

## 기본 정보

| 항목 | 값 |
|---|---|
| **앱 이름 (App Name)** | `원터치맵` |
| **부제 (Subtitle, 30자)** | `배송 주소 인식·경로·지도` |
| **번들 ID** | `com.avabag01.onetouchmap` |
| **기본 언어** | 한국어 |
| **카테고리 (기본)** | 내비게이션 |
| **카테고리 (보조)** | 비즈니스 |
| **연령 등급** | 4+ |
| **저작권 (Copyright)** | `2026 youngil kim` |

> ⚠️ **앱 이름 중복 주의:** "원터치맵"이 이미 선점됐으면 App Store Connect가 거부해. 대안: `원터치맵 - 배송 주소`, `원터치맵 배송`, `원터치 배송맵`.

---

## 프로모션 텍스트 (Promotional Text, 170자 — 나중에 수시 변경 가능)

```
배송전표를 사진 한 장으로! OCR로 주소를 자동 인식하고, 구주소↔신주소 변환과 지도·경로까지 한 번에. 택배·배송 기사를 위한 가장 빠른 주소 도구.
```

---

## 설명 (Description, 4000자 이내)

```
원터치맵은 택배·배송 기사를 위한 주소 관리 앱입니다.
배송전표를 촬영하면 OCR이 주소를 자동으로 읽어들이고, 구주소와 신주소(도로명)를 즉시 변환해 줍니다. 입력한 배송지는 지도에 표시되고, 현재 위치 기반으로 경로를 확인할 수 있습니다.

■ 주요 기능
· 배송전표 OCR 인식 — 전표를 사진 찍으면 주소가 자동 입력 (기기 내 처리, 서버 전송 없음)
· 주소 변환 — 지번(구주소) ↔ 도로명(신주소) 실시간 변환
· 동 선택 → 도로명 자동 표시 — 원하는 동을 고르면 그 지역 도로명이 바로 떠서 빠르게 입력
· 배송 목록 관리 — 오늘의 배송지를 목록으로 정리
· 지도 표시 — 배송지를 지도에서 한눈에 확인
· 현재 위치 기반 안내

■ 이런 분께 좋아요
· 하루에 수십 곳을 도는 택배·배송 기사
· 전표의 주소를 일일이 타이핑하기 번거로운 분
· 구주소/신주소를 자주 바꿔 확인해야 하는 분

■ 개인정보 보호
· 입력한 배송지와 사진은 기기 안에만 저장되며 외부 서버로 전송되지 않습니다.
· 주소 검색은 국토교통부 공공 API(VWorld)를 통해 이루어집니다.
· 앱을 삭제하면 모든 데이터가 함께 삭제됩니다.
```

---

## 키워드 (Keywords, 쉼표로 구분, 총 100자 이내)

```
배송,택배,주소,전표,OCR,도로명,지번,신주소,구주소,경로,지도,배달,물류,내비,주소변환
```

---

## URL

| 항목 | 값 | 상태 |
|---|---|---|
| **개인정보처리방침 URL** (필수) | `https://avabag01-ai.github.io/one-touch-map/privacy.html` | ⚠️ GitHub Pages 켜야 활성화 (아래 체크리스트 참고) |
| **지원 URL** (필수) | `https://github.com/avabag01-ai/one-touch-map` | 저장소 공개 상태면 그대로 사용 가능 |
| **마케팅 URL** (선택) | (비워도 됨) | — |

---

## App 개인정보 보호 (App Privacy) 설문 답변 권장값

App Store Connect의 "앱 개인정보 보호" 질문에 대한 권장 답변:

- **데이터를 수집합니까?** → 이 앱은 사용자 데이터를 **자체 서버로 수집·전송하지 않음**. 배송지·사진·위치 모두 기기 내에서만 사용/저장.
  - 가장 단순하고 사실에 맞는 답: **"데이터를 수집하지 않습니다 (No, we do not collect data)"**
  - 근거: OCR은 기기 내 처리(Tesseract.js), 데이터는 localStorage에만 저장, 서버 없음. VWorld는 주소 검색용 기능성 API 호출(제3자 서버에 사용자 계정·식별자를 축적하지 않음).
- 만약 심사에서 위치/사용자콘텐츠 관련 추가 질문이 오면: "앱 기능(App Functionality) 목적, 사용자 식별과 연결 안 됨, 추적 안 함"으로 답.

> 개인정보처리방침 전문은 저장소의 `privacy.html`에 있음 (수집 항목·목적·보유기간·제3자 제공·문의 이메일 포함).

---

## 심사 정보 (App Review Information)

- **연락처:** 이름 / 전화 / 이메일 `avabag01@gmail.com`
- **데모 계정:** 불필요 (로그인 없는 앱)
- **비고(Notes) 예시:**
  ```
  이 앱은 로그인이 없으며 모든 데이터는 기기 내 localStorage에만 저장됩니다.
  주소 검색은 국토교통부 VWorld 공개 API를 사용합니다.
  카메라/사진 권한은 배송전표 OCR(기기 내 처리)에만 사용됩니다.
  ```

---

## 스크린샷 (직접 캡처 필요)

App Store는 최소 **6.7"(또는 6.9") iPhone** 스크린샷이 필요 (1~10장).
- 실기기(iPhone 15 Plus = 6.7")에서 앱 실행 → 스크린샷 촬영 → 그대로 업로드 가능
- 추천 화면: ① 메인 주소 입력/키패드 ② 배송 목록 ③ 지도 화면 ④ (선택) OCR 전표 인식
- iPad 지원 표기가 있으므로, iPad 스크린샷도 요구될 수 있음 (없으면 iPad 지원 끄는 것도 방법)
